# conftest.py
# Pytest root configuration for the tests/ directory.
# This file allows pytest to discover tests without requiring
# sys.path manipulation in each test file.
# When running pytest from the project root (CAR_MuSC/), all
# imports resolve correctly via the installed package structure.
